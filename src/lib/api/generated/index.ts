// Placeholder file for generated API client
// Run `npm run generate:api` to generate the API client from your backend's OpenAPI specification

/**
 * Temporary Configuration class stub
 * This will be replaced when you run `npm run generate:api`
 */
export class Configuration {
    basePath?: string;
    headers?: Record<string, string>;

    constructor(params?: { basePath?: string; headers?: Record<string, string> }) {
        this.basePath = params?.basePath;
        this.headers = params?.headers;
    }
}

/**
 * Temporary API class stub
 * This will be replaced when you run `npm run generate:api`
 */
export class DefaultApi {
    constructor(_config?: Configuration) {
        // Temporary implementation
    }
}

